import React from 'react';
import { Platform, StatusBar, StyleSheet, View } from 'react-native';
class ArtGame extends React.Component{
  render(){
    const options=()=>{
      var optionsArray = [];
      var correctOptionKey = 0;
      var const = {
        options: ["Can","Can1","Can2","Can3"],
      }
    };
    return(options.const[options]);
    }
}
export default ArtGame;
